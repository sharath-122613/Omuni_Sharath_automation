# Database Configuration
# SSH Tunnel Configuration
ssh.tunnel.enabled=true
ssh.tunnel.host=bastion-preprod.omuni.com
ssh.tunnel.port=2040
ssh.tunnel.username=sharath.km
ssh.tunnel.password=asdfghjkl
ssh.tunnel.local-port=33306  # Local port for SSH tunnel

# Database Connection
spring.datasource.url=jdbc:mysql://localhost:33306/nickfury?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=nickfury
spring.datasource.password=mwKGFy3T6SBZ
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Properties
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Connection Pool Settings
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.com.cloud.omuni_cloud=DEBUG

# JPA Entity Scan
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.properties.hibernate.default_schema=${db.name}

# Disable JPA auto-ddl for production
spring.jpa.hibernate.ddl-auto=validate